import 'package:flutter/material.dart';

class NightLife extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text('Night Life\n\nqui metteremo la lista delle serate prova prova'));
  }
}