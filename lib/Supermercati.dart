// DevelopmentInfoPage.dart
import 'package:flutter/material.dart';

class Supermercati extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Informazioni sui supermercati'),
      ),
      body: Center(
        child: Text('oppure no'),
      ),
    );
  }
}
