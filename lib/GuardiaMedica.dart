// DevelopmentInfoPage.dart
import 'package:flutter/material.dart';

class GuardiaMedica extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Informazioni sulla guardia medica'),
      ),
      body: Center(
        child: Text('Anche pronto soccorso'),
      ),
    );
  }
}
